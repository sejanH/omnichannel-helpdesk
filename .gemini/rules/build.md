# Asset Compilation & Dev Server

- Do NOT execute `npm run build` automatically after making frontend, CSS, or Blade template changes in this project.
- The user is actively running `npm run dev` in their terminal for instant hot reloading and asset serving.
